# This library processes the runs and channel structure neccesary for everything else
# Simplicity is the key

import numpy as np
import pandas as pd

# Use main dir as system path
import sys
sys.path.insert(0, '../')


# def open_run(run,channels,run_path="your_path_goes_here_hehehe"):
    
#     out_folder = "run"+str(run).zfill(2)+"_ch"+str(ch)+"/"
#     for ch in channels:
        
#         np.load( ,allow_pickle=True) ["arr_0"]